{
  "description": {
    "default": "## Error Handling Standards\n\n- Never swallow exceptions silently — always log or re-throw.\n- Use typed error classes or error codes for predictable error flows.\n- Validate inputs at system boundaries (API endpoints, user inputs).\n- Implement graceful degradation over hard crashes.\n- Return meaningful error messages to callers without exposing internals."
  }
}
